import requests
#request = requests.get('https://www.domain.com/')
request = requests.get('https://www.domain.com/', cookies={"name" : "Value" }).text
print(request)
------------------------------------

# import re
# re.findall('flag{.*}')

-----------------------------------
# # hex decoder python
# bytes.fromhex('68656c6c6f').decode('utf-8')
